# Render/Heroku set NODE_ENV=production during build, which skips devDependencies
# and breaks `nest build`. Always install devDependencies so the default
# `npm install && npm run build` path works on free-tier Node services.
production=false
